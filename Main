@keyframes floatBounce {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  50% { transform: translateY(-8px) rotate(1.5deg); }
}

@keyframes cuteWiggle {
  0%, 100% { transform: rotate(0deg) scale(1); }
  25% { transform: rotate(-2deg) scale(1.02); }
  75% { transform: rotate(2deg) scale(1.02); }
}

@keyframes sparklePop {
  0% { transform: scale(0) translateY(0); opacity: 1; }
  100% { transform: scale(1.4) translateY(-25px); opacity: 0; }
}

@keyframes musicSpin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.animate-float-bounce { animation: floatBounce 4s ease-in-out infinite; }
.animate-cute-wiggle { animation: cuteWiggle 6s ease-in-out infinite; }
.animate-music-spin { animation: musicSpin 4s linear infinite; }
.sparkle-pop { animation: sparklePop 0.8s ease-out forwards; }

.glass-panel-dark {
  background: rgba(15, 18, 26, 0.88);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(239, 68, 68, 0.25);
}

.glass-panel-light {
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(234, 179, 8, 0.35);
  box-shadow: 0 8px 24px rgba(0,0,0,0.06);
}
